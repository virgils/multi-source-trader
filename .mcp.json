{
  "mcpServers": {
    "octagon": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "octagon-mcp@latest"],
      "env": {
        "OCTAGON_API_KEY": "${OCTAGON_API_KEY}"
      }
    },
    "massive": {
      "type": "stdio",
      "command": "mcp_massive",
      "env": {
        "MASSIVE_API_KEY": "${MASSIVE_API_KEY}"
      }
    },
    "robinhood": {
      "type": "http",
      "url": "${ROBINHOOD_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${ROBINHOOD_API_KEY}"
      }
    }
  }
}
